"""Credit-risk models ported from the HSF toolbox (`hfs-archive`'s
`HSF/0. Toolbox/credit/`): structural (asset-value) default models
(`structural.py`) and reduced-form (intensity/hazard-based) default
models (`reduced_form.py`). See docs/migration_map.md, "HSF toolbox
port", for the full source breakdown.
"""
