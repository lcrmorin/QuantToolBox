"""Special matrix operations: vec/vech/xpnd, commutation/duplication/elimination matrices.

Maps from: QuantToolbox/matrix/*.m
Simple wrappers (rows, cols, sumc, meanc, packr, selif, seqa, lag1, ...)
are NOT ported -- replaced throughout by native NumPy/pandas calls.
"""
