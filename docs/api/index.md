# API Reference

Auto-generated from the docstrings throughout the package. Every
function includes the original MATLAB source file(s) it was ported
from, in an `Original:` line at the top of its docstring.

Pages so far cover the modules with worked examples; the rest of the
package (`dates`, `backtest`, `optim`, `svm`, `spline`, `maths`,
`mixtures`, `linalg`) follows the same `::: module.path` pattern below
and can be added the same way.
